---
import Layout from '../../layouts/Layout.astro';

const base = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : import.meta.env.BASE_URL + '/';
---

<Layout title="方法论 — AI PM 工作台" lang="zh-CN" description="AI PM 工作台背后的思维框架">
  <main class="max-w-4xl mx-auto px-4 py-12">
    <h1 class="text-3xl font-bold mb-3">方法论</h1>
    <p class="text-muted-foreground mb-10 max-w-2xl">工作台如何构建 AI 产品思维——从问题定义到交付物生成。</p>

    <div class="space-y-8">
      <section>
        <h2 class="text-xl font-semibold mb-3">核心原则</h2>
        <p class="text-muted-foreground">大多数 AI 产品失败的根源在于从技术出发而非从问题出发。工作台强制执行结构化的推进路径：先定义问题和用户，再选择 AI 能力，然后设定评估标准，最后才生成交付物。</p>
      </section>

      <section>
        <h2 class="text-xl font-semibold mb-3">四步工作流</h2>
        <div class="space-y-4">
          <div class="bg-card rounded-xl ring-1 ring-foreground/10 p-5">
            <h3 class="font-medium mb-1">1. 产品定义</h3>
            <p class="text-sm text-muted-foreground">定义业务场景、目标用户、当前工作流和知识上下文。这一步可以防止为不存在的问题构建 AI。</p>
          </div>
          <div class="bg-card rounded-xl ring-1 ring-foreground/10 p-5">
            <h3 class="font-medium mb-1">2. 设计智能能力</h3>
            <p class="text-sm text-muted-foreground">选择合适的 AI 能力（分类、生成、RAG、智能体）并设计工作流边界。这一步回答：这个场景需要智能体，还是更简单的方案就够了？</p>
          </div>
          <div class="bg-card rounded-xl ring-1 ring-foreground/10 p-5">
            <h3 class="font-medium mb-1">3. 评估与风险</h3>
            <p class="text-sm text-muted-foreground">设定评估指标、验收标准，并识别生产环境风险。建议标签帮助你覆盖可能遗漏的指标。</p>
          </div>
          <div class="bg-card rounded-xl ring-1 ring-foreground/10 p-5">
            <h3 class="font-medium mb-1">4. 导出交付物</h3>
            <p class="text-sm text-muted-foreground">生成 6 份结构化文档：产品简报、工作流规格、评估计划、验收标准、编码智能体交接文档和 CLAUDE.md 初始模板。支持复制、下载或继续使用 AI。</p>
          </div>
        </div>
      </section>

      <section>
        <h2 class="text-xl font-semibold mb-3">关键概念</h2>
        <div class="grid gap-4 md:grid-cols-2">
          <div class="bg-card rounded-xl ring-1 ring-foreground/10 p-5">
            <h3 class="font-medium mb-1">Vibe Coding</h3>
            <p class="text-sm text-muted-foreground">利用编码智能体将产品意图转化为原型。PM 聚焦于架构和评估，而非实现细节。</p>
          </div>
          <div class="bg-card rounded-xl ring-1 ring-foreground/10 p-5">
            <h3 class="font-medium mb-1">Harness Engineering</h3>
            <p class="text-sm text-muted-foreground">为编码智能体设计上下文、工具、约束、检查和反馈系统——而不是自己编写代码。</p>
          </div>
          <div class="bg-card rounded-xl ring-1 ring-foreground/10 p-5">
            <h3 class="font-medium mb-1">Agent 与非 Agent</h3>
            <p class="text-sm text-muted-foreground">并非每个 AI 产品都需要智能体。工作台帮助你判断：多步推理和工具使用才需要智能体；确定性规则和简单检索则不需要。</p>
          </div>
          <div class="bg-card rounded-xl ring-1 ring-foreground/10 p-5">
            <h3 class="font-medium mb-1">隐私优先</h3>
            <p class="text-sm text-muted-foreground">所有数据都保存在浏览器中。没有服务器、没有 API 调用、没有账户。你的产品创意永远不会离开你的设备。</p>
          </div>
        </div>
      </section>

      <section>
        <h2 class="text-xl font-semibold mb-3">资源</h2>
        <ul class="space-y-2 text-muted-foreground">
          <li><a href={`${base}zh-CN/ai-pm-role/`} class="text-emerald-400 hover:text-emerald-300 transition-colors">AI PM 角色 &rarr;</a> — 成为 AI PM 意味着什么</li>
          <li><a href={`${base}zh-CN/vibe-coding/`} class="text-emerald-400 hover:text-emerald-300 transition-colors">Vibe Coding &rarr;</a> — PM 与编码智能体的协作</li>
          <li><a href={`${base}zh-CN/harness-engineering/`} class="text-emerald-400 hover:text-emerald-300 transition-colors">Harness Engineering &rarr;</a> — 引导 AI 编写代码</li>
          <li><a href={`${base}zh-CN/prompt-to-ontology/`} class="text-emerald-400 hover:text-emerald-300 transition-colors">Prompt-to-Ontology 案例 &rarr;</a> — 真实项目示例</li>
        </ul>
      </section>
    </div>

    <!-- CTA -->
    <div class="mt-10 p-5 rounded-xl ring-1 ring-emerald-500/20 bg-emerald-500/5 text-center">
      <p class="text-sm text-muted-foreground mb-3">在 AI PM 工作台中实践这些概念</p>
      <a href={`${base}zh-CN/workbench/`} class="inline-flex items-center gap-2 px-5 py-2.5 rounded-lg bg-emerald-600 hover:bg-emerald-500 text-white text-sm font-medium transition-colors no-underline">
        打开工作台
      </a>
    </div>
  </main>
</Layout>
